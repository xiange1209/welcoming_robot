# 驗證與實作清單

更新日期：2026-07-06

> **2026-07-06 更新**：WHEELTEC 廠商程式碼整合完成——精選 17 套件（Jazzy 修正後）放入 `src/`，bringup 新增 `enable_chassis` / `audio_stack` / `llm_stack` 參數。驗證步驟見 B11。
> **2026-07-08 更新**：全 workspace 已在 **x86-64 虛擬機（Ubuntu 24.04.4 + ROS 2 Jazzy）** `colcon build` 通過（24/24 套件；`wheeltec_mic_ros2` 綁 arm64 預編譯庫，僅能在 arm64 建置）＋launch 解析/啟動 smoke test 通過。過程修正 4 類 Jazzy/gcc-13 相容問題（見 commit `0039541`＋`6186c3a`）。
> **2026-07-09 更新**：**arm64 全 workspace 建置通過（25/25，含 wheeltec_mic_ros2）**——QEMU `ros:jazzy` arm64 容器（部署指南路線 D），三個關鍵執行檔以 `file` 驗證為 aarch64，產物 `smartnav_arm64_install.tar.gz`（42MB）可直接搬 RPi4 執行。過程再修 2 個問題：`.gitignore` 舊規則吃掉廠商 arm64 函式庫（commit `b337e7d`）、lslidar_driver 執行檔缺 PCL 明確連結（arm64 ld 不容忍傳遞依賴）。**硬體驗證（B11 (2)~(6)）仍未做**。

這份清單的目的，是把你接下來要做的事情拆成可以直接執行、直接回報結果的項目。

> **2026-07-02 更新**：專案已重組——
> 1. ROS2 套件已從 `smartnav-bot/src/smartnav_ros2/` 上移到 `src/` 直下，`src/` 本身就是 ROS2 workspace 的 source 目錄。
> 2. 舊的單機 Python 原型已全部刪除（`src/ai_vision/`、`src/database/`、`src/llm_server/`、`src/scripts/` 等），本文件所有指令已改為 ROS2 版。
> 3. 已跟上孫同學 2026-07-01 的最新拆分：導航相關內容從 smartnav_brain 拆出成 `smartnav_navigation`，smartnav_brain 與 smartnav_hmi 目前是保留給未來的空殼套件。
> 4. B10 的 LLM 延遲基準測試已於 2026-07-02 在筆電（RTX 3050 4GB）實測完成，結果見 B10。

## A. 這週最優先

### A1. 完成人臉辨識端對端驗證（ROS2 版）

目標：確認透過 `/face_registration/register` 服務註冊後，`face_recognition` 節點能正確辨識 VIP、黑名單、訪客，並發布到 `/face_recognition/result`。

執行順序：

- [ ] 在 RPi4 上 `colcon build --symlink-install` 並 `source install/setup.bash`
- [ ] 確認有相機節點在發布 `/image_raw`（若話題不同，用 `image_topic` 參數對齊）
- [ ] 啟動 `face_recognition` 節點（開啟除錯影像）
- [ ] 啟動 `face_registration` 節點
- [ ] 用服務呼叫註冊 1 位 VIP（本人站在鏡頭前，節點會採集 `num_samples` 張樣本）
- [ ] 用服務呼叫註冊 1 位黑名單
- [ ] 用 `ros2 topic echo /face_recognition/result` 驗證 VIP / 黑名單 / 陌生訪客三種判定
- [ ] 用 `rqt_image_view` 看 `/face_recognition/debug_image` 確認框色與標籤（VIP=綠框、黑名單=紅框、訪客=藍框）

建議命令：

```bash
# 建置與環境
cd ~/專題        # workspace 根目錄（內含 src/）
colcon build --symlink-install
source install/setup.bash

# 啟動人臉辨識節點（開啟除錯影像發布）
ros2 run smartnav_vision face_recognition --ros-args -p publish_debug_image:=true

# 另開終端機：啟動人臉註冊節點
ros2 run smartnav_vision face_registration

# 註冊 VIP（受測者需站在鏡頭前）
ros2 service call /face_registration/register smartnav_msgs/srv/RegisterFace \
  "{person_name: '測試VIP', gender: 'M', person_type: 'VIP', num_samples: 10}"

# 註冊黑名單
ros2 service call /face_registration/register smartnav_msgs/srv/RegisterFace \
  "{person_name: '測試黑名單', gender: 'M', person_type: 'BLACKLIST', num_samples: 10}"

# 若手動改過資料庫，可要求辨識節點重載快取
ros2 service call /face_recognition/refresh_cache std_srvs/srv/Empty

# 觀察辨識結果（person_uuid / person_name / gender / person_type / confidence / bbox）
ros2 topic echo /face_recognition/result

# 觀察除錯影像
ros2 run rqt_image_view rqt_image_view /face_recognition/debug_image
```

驗證證據至少要留：

- [ ] `/face_recognition/result` 的 echo 輸出（VIP、黑名單、訪客各一段）
- [ ] `/face_recognition/debug_image` 畫面截圖
- [ ] 誤判或漏判案例紀錄
- [ ] 當下使用的閾值（`recognition_threshold`，預設 0.6；`confidence_threshold`，預設 0.5）

### A2. 完成 RPi4 smartnav_llm → 筆電 Ollama 驗證

目標：確認 RPi4 上的 `smartnav_llm` 節點能透過區網連上筆電的 Ollama，並對 `/user_text` 的輸入產生回應。

執行順序：

- [ ] 筆電上設定 `OLLAMA_HOST=0.0.0.0` 並重啟 Ollama（讓區網可連）
- [ ] 筆電本機用 `curl http://localhost:11434/api/tags` 確認 Ollama 正常、模型已 pull
- [ ] RPi4 上 `curl http://<筆電IP>:11434/api/tags` 確認跨機連線可通（注意 Windows 防火牆需放行 11434）
- [ ] RPi4 上啟動 `smartnav_llm`，`ollama_base_url` 指向筆電
- [ ] 對 `/user_text` 發布測試訊息，觀察 llm_service_node 的回應與工具呼叫行為
- [ ] 記錄回應延遲與失敗情況（斷線、逾時時節點的行為）

建議命令：

```powershell
# 筆電（Windows PowerShell）
$env:OLLAMA_HOST = "0.0.0.0"
ollama serve          # 或重啟 Ollama 服務
curl http://localhost:11434/api/tags
```

```bash
# RPi4
curl http://<筆電IP>:11434/api/tags

# 啟動 LLM 節點（依 B10 實測結果，建議先用 qwen2.5:3b）
ros2 launch smartnav_llm llm.launch.py \
  ollama_base_url:=http://<筆電IP>:11434 \
  model_name:=qwen2.5:3b

# 另開終端機：發送測試文字
ros2 topic pub --once /user_text std_msgs/msg/String "{data: '你好，請介紹你自己'}"
```

驗證證據至少要留：

- [ ] RPi4 上 `curl /api/tags` 成功回應
- [ ] llm_service_node 啟動與服務就緒的 log
- [ ] `/user_text` 測試訊息對應的 LLM 回應內容
- [ ] 回應延遲數據，以及 Ollama 不可達時節點的錯誤行為

## B. 接下來的實作順序（2026-07-02 更新：套件已上移到 src/ 直下，重點是驗證與整合）

### B1. ROS2 workspace 可建置性驗證（優先）

- [ ] 在 ROS2 Jazzy 環境執行 `rosdep install --from-paths src --ignore-src -r -y`
- [ ] 執行 `colcon build --symlink-install`，確認 smartnav_msgs 能成功產生介面
- [ ] 確認 smartnav_vision、smartnav_audio、smartnav_llm、smartnav_navigation、smartnav_brain、smartnav_hmi、smartnav_bringup 七個套件都能成功建置
- [ ] `frontier_exploration_ros2` 不在本 repo 內（同學 repo 用 git submodule，本 repo 尚未加），需手動 clone 到 `src/`：`git clone https://github.com/mertgulerx/frontier_exploration_ros2 src/frontier_exploration_ros2`
- [ ] 記錄下缺少的 rosdep 依賴（特別是 frontier_exploration_ros2，它不一定能由標準 rosdep 解析）

### B2. smartnav_vision 版本（已決策，只需驗證）

已確認本專案 smartnav_vision 是以孫同學實際維護的 GitHub 版本（swient/smartnav-bot）為基礎，正確疊加 gender/person_type 銀行場景擴充，**不是需要重新決策的分岔版**。setup.py/package.xml 的相依宣告已補齊。

- [ ] 確認本專案版 face_recognition_node.py 能否在 ROS2 環境正常訂閱 `/image_raw` 並發布辨識結果
- [ ] 確認 RegisterFace.srv（本專案版，含 gender/person_type）與 face_registration_node.py 一致

### B3. smartnav_audio 語音驗證（已有實作）

- [ ] 執行 `scripts/download_audio_models.sh` 下載 sherpa-onnx 的 KWS、ASR、VAD、TTS 模型
- [ ] 驗證 voice_trigger（喚醒詞）能否正常偵測語音起始
- [ ] 驗證 speech_recognizer（ASR）輸出文字是否正確，並確認是否支援多語言
- [ ] 驗證 speech_synthesizer（TTS）能否正常合成語音、voice_playback 能否正常播放
- [ ] 記錄 RPi4 上的實際延遲與 CPU/記憶體使用率

### B4. smartnav_navigation 地圖與導航驗證（原 smartnav_brain 導航內容，2026-07-01 已拆出）

- [ ] 確認 Nav2、slam_toolbox、frontier_exploration_ros2 已安裝（frontier_exploration_ros2 見 B1，需手動 clone）
- [ ] 執行 `ros2 launch smartnav_navigation brain.launch.py`，確認 map_service / waypoint_service / navigation_action 都能正常啟動
- [ ] 呼叫 CreateMap action，驗證自動探索建圖是否可用
- [ ] 呼叫 Navigate action，驗證能否導航到已登記地點
- [ ] 如果沒有 TurtleBot3 實體，先用 `nav2.launch.py` 搭配 turtlebot3_gazebo 模擬驗證

### B5. smartnav_llm Agent 驗證（終局 LLM 架構，優先）

- [ ] 在筆電確認模型已拉取（依 B10 實測，建議 `ollama pull qwen2.5:3b`；launch 預設的 gemma4:e2b 在 RTX 3050 4GB 上會卸載到 CPU，回應要 40 秒以上，不適合即時互動）
- [ ] 使用 `ros2 launch smartnav_llm llm.launch.py ollama_base_url:=http://<筆電IP>:11434 model_name:=qwen2.5:3b` 啟動，確認能連上筆電的 Ollama
- [ ] 確認能連上 list_maps、create_waypoint、navigate 等服務/動作
- [ ] 發送自然語言指令到 `/user_text`，確認 Agent 能正確選擇工具並呼叫對應 ROS2 服務
- [ ] 記錄回應延遲與工具呼叫成功率

### B6. smartnav_vision 接上 smartnav_llm（已實作，待驗證，2026-07-01）

已按確認的設計完成實作：ROS2 topic 串接，並維持 smartnav_llm 現有的文字輸入方式。

- [x] `face_recognition_node.py` 發布 `/face_recognition/result`（smartnav_msgs/RecognitionResult），身分改變時立即發布，同身分持續則每 `result_republish_interval`（預設 15 秒）重新發布一次
- [x] `recognition_text_bridge_node.py`（smartnav_vision 套件內），訂閱該結果、轉成自然語言文字，發布到 `/user_text`
- [ ] 實機驗證：註冊一位 VIP 後，確認辨識到時 `/user_text` 會收到對應文字，且 smartnav_llm 會回應
- [ ] 實機驗證：同一人持續出現在鏡頭前時，不會每幀都發送，但會按間隔重新發送

> **已知限制**：目前 smartnav_llm 的工具集（create_map / list_maps / switch_map / create_waypoint / list_waypoints / navigate / global_localization）全部是導航/地圖導向，**沒有銀行場景專用工具**（例如「帶領 VIP 到貴賓室」「通知工作人員」），system_prompt.txt 也是純導航機器人設定。人臉辨識事件進入 `/user_text` 後，Agent 只會當作一般對話回應，不會自動觸發銀行專屬行為。若要真正實現「VIP 自動帶領」「黑名單自動通報」，還需新增對應工具與修改 system_prompt.txt，目前尚未做。

### B7. smartnav_bringup 總入口（已實作，待驗證）

- [x] CMakeLists.txt 新增 `install(DIRECTORY launch ...)`
- [x] package.xml 新增對 smartnav_vision/audio/llm/navigation 的 exec_depend
- [x] `launch/smartnav.launch.py`，可用 `enable_vision` / `enable_audio` / `enable_llm` / `enable_navigation` / `enable_nav2` 分別開關各模組，並可傳入 `image_topic` / `enable_gpu` / `ollama_base_url` / `model_name` / `use_sim_time`
- [x] （2026-07-06）新增 `enable_chassis`（WHEELTEC 底盤，預設 false）、`audio_stack:=smartnav|wheeltec`、`llm_stack:=smartnav|wheeltec` 三個參數，雙版本方案可切換
- [ ] 實機驗證：`colcon build` 能否成功建置 smartnav_bringup
- [ ] 實機驗證：`ros2 launch smartnav_bringup smartnav.launch.py ollama_base_url:=http://<筆電IP>:11434` 能否正常啟動 vision + audio + llm + navigation
- [ ] 如果沒有麥克風/鏡頭硬體，先用 `enable_audio:=false` 或 `enable_vision:=false` 分別關閉，單獨驗證其他模組
- [ ] `enable_nav2` 預設關閉，待有 TurtleBot3 實體或 Gazebo 模擬環境後再打開驗證

### B11. WHEELTEC 整合驗證（2026-07-06 已實作，現在的最優先）

整合內容：`src/` 17 個廠商套件（Jazzy 修正後）＋bringup 三個新參數＋`ollama_chat_bridge` 橋接節點。

- [x] **(1a) 全 workspace 建置——x86-64 VM**（2026-07-08 通過）：Ubuntu 24.04.4 + Jazzy 虛擬機上 24/24 套件建置成功、`ros2 launch smartnav_bringup smartnav.launch.py -s` 解析正常、全關模式啟動乾淨退出。例外：`wheeltec_mic_ros2` 連結廠商 arm64 專用 `liboffline_record_lib`，x86 上用 `--packages-skip wheeltec_mic_ros2` 跳過。
- [x] **(1b) 全 workspace 建置——arm64**（2026-07-09 通過，QEMU 容器）：25/25 含 wheeltec_mic_ros2，執行檔以 `file` 驗證為 aarch64。產物 `smartnav_arm64_install.tar.gz` 解壓到 RPi4 即用（部署指南路線 D5），**RPi4 不需自行編譯**；要原生編譯見路線 D6（限平行度＋swap）。
- [x] **(1c) RPi4 實機首次啟動**（2026-07-10 通過，真實 RPi4B 4GB `wheeltec-r680`，Ubuntu 24.04.4 + Jazzy）：arm64 產物解壓即用——`ros2 pkg list` 8 個 smartnav 套件全可見；`ros2 launch smartnav_hmi hmi.launch.py` 成功（網頁 HTTP 200、標題正確、rosbridge :9090 與 http :8081 監聽中）；`web_video_server` HTTP 200；執行檔 `file` 驗證 aarch64。workspace 在 `~/smartnav_ws`（git clone＋tarball 解壓），meshes 已從舊映像搬入並 symlink 進 install share，`.bashrc` 已改 source 新 workspace。apt 已補 rosbridge-server/web-video-server/teleop/libuvc-dev/glog-dev，pip 已裝 openai 2.45.0（需 `--ignore-installed typing_extensions` 繞過 Debian 管理套件）。**尚缺 vision/audio 的 Python 依賴**（insightface、onnxruntime、sherpa-onnx、langchain 未裝，測 (3)(4) 前要裝）。
- [~] **(2) 底盤 smoke test**（2026-07-21 唯讀部分通過，遙控待測）：實車 Pi 上 `/dev/wheeltec_controller -> ttyUSB1` 由 udev 正確建立；`car_mode` 原為 `mini_mec`，經使用者確認為阿克曼車後已改 `senior_akm`（log 顯示 `car_mode:senior_akm`）；`wheeltec_robot serial port opened`＋`Data ready`，`/odom` 與 EKF `/odom_combined`（frame `odom_combined` → `base_footprint`）皆有資料、TF 樹完整。**仍待做**：把車架高 → `ros2 topic pub /cmd_vel ...` 確認輪子轉與前輪打角（阿克曼車：`angular.z` 是等效轉向、不會原地轉）
- [x] **(2b) N10 光達**（2026-07-21 通過）：設定檔原為 `lidar_type: ls_N10Plus_uart`，但直接量測序列埠證實實體光達是 **N10（230400 baud、封包標頭 `A5 5A`、固定間距 58 bytes、一致性 100%）**；改為 `ls_N10_uart` 後 `/scan` 正常——**10.06 Hz、std dev 0.0013s、360°（±π）、angle_increment 0.01396 rad（約 450 點）**，實測距離 0.36～0.88m。備份：`wheeltec_param.yaml.bak-20260721`
- [ ] **(3) wheeltec 語音鏈**：`audio_stack:=wheeltec` → 喚醒詞喚醒 → 說命令詞 → `ros2 topic echo /user_text` 收到文字（注意：離線命令詞是固定語法，非自由語句）
- [ ] **(4) wheeltec LLM 鏈**：`llm_stack:=wheeltec ollama_base_url:=http://<筆電IP>:11434 model_name:=qwen2.5:3b` → `ros2 topic pub /user_text std_msgs/String "{data: '你好'}"` → `ros2 topic echo /llm_response` 與 `/speech_text` 有完整回覆
- [ ] **(5) 建圖→導航（wheeltec 官方鏈，不經 bringup）**：`ros2 launch wheeltec_slam_toolbox online_async_launch.py` 建圖 → 存圖 → `ros2 launch wheeltec_nav2 wheeltec_nav2.launch.py`。**警告**：此 launch 自帶底盤啟動，不可與 `enable_chassis:=true` 同開；WHEELTEC 鏈用 `odom_combined` frame，與 smartnav_navigation（`odom`）不可混用
- [x] **(6) astra 相機**（2026-07-21 通過，★閉源驅動在 Ubuntu 24.04 的相容性疑慮解除）：`lsusb` 認出 `2bc5:0402 Orbbec ASTRA S`；`Device connected: Astra serial number: 17121611557`；彩色 640×480 `rgb8`、深度 640×480 `16UC1`，**實測彩色 20.8 Hz、深度 26.7 Hz**，`/camera/depth/points` 點雲正常。IR 串流與彩色互斥被自動停用（廠商設計）。smartnav_vision 要接它時用 launch remap `-r /image_raw:=/camera/color/image_raw`（零成本，勿用 topic_tools relay——Pi4 上 640×480@30fps 轉發要多花約 27MB/s 記憶體複製）。⚠️ 同時跑兩個 `astra_camera_node` 會搶 USB 導致彩色斷流，啟動前確認 `ps -eo comm | grep -c astra_camera` 為 1
- [x] meshes 不在 git 內：2026-07-10 已從 Pi 舊 workspace 原地 `mv` 到 `~/smartnav_ws/src/wheeltec_robot_urdf/meshes`（1.2GB）並 symlink 進 install share；日後重灌才需要重新 scp（RViz 顯示模型才需要，導航不需要）

### B8. 銀行知識與金融資訊

- [x] 定義銀行 FAQ / 服務問題範圍（2026-07-17：`smartnav_llm/config/bank_faq.txt` 示範版——營業時間/開戶/匯兌/貴賓室/貸款；正式展示前依實際設定改內容）
- [ ] 接入匯率或金融資訊來源（Phase C 可選）
- [x] 讓 LLM 能回答銀行相關問題（2026-07-17：`query_bank_faq_tool` 先查再答＋銀行版 system prompt 禁止編造數字——**待實機驗證**）

### B12. 銀行迎賓與通報（2026-07-17 實作，2026-07-21 實機驗證——記錄見 docs/測試記錄_20260720.md）

- [x] **(1) 劇本模擬測試**（2026-07-21 全數通過，於車上 Pi 實機）：VIP 迎賓詞（`陳佳憲貴賓您好，歡迎蒞臨，需要為您帶位到貴賓室嗎`）、BLACKLIST dry-run 通報 log、VISITOR 一般歡迎詞、**低信心 0.30 正確被 `min_confidence` 0.5 擋下**、60 秒冷卻防抖（重發同 uuid 無輸出且無重複記錄）、`/brain_event` 結構化 JSON、SQLite 三筆記錄、通報中樞 `/staff_notify_request` 轉發正常
- [ ] **(2) Telegram 真推播**：建 bot → 帶 token/chat_id 啟動 → 黑名單事件 → 手機收到推播（2026-07-21 使用者選擇跳過；dry-run 已證明通報邏輯正確，只差真實後端）
- [ ] **(3) LLM 銀行工具**：`/user_text` 問營業時間 → FAQ 工具正確呼叫；「帶我去貴賓室」→ guide 工具（無導航時回覆如實失敗）。**卡在 Pi 上 langchain 未安裝**
- [ ] **(4) 實車整合（T7）**：真相機辨識觸發完整迎賓（相機已通過 B11(6)，尚缺 insightface/onnxruntime 依賴＋人臉註冊）
- [ ] **(5) 帶位端到端**：建「貴賓室」地點 → LLM 帶位導航實走
- [x] **(6) 關閉行為修正**（2026-07-21）：`bank_reception_node` 收 SIGTERM 時噴 `ExternalShutdownException`＋`RCLError` traceback，已修（捕捉例外＋`if rclpy.ok()` 保護）並實機驗證。**同樣瑕疵仍存在於另外 11 個節點**，待統一掃除（`smartnav_vision` 兩個節點是正確樣板）

### B9. UI 與機器人整合

- [x] 定義機器人端 UI 需要顯示的狀態（2026-07-10：smartnav_hmi 已實作為平板網頁 UI——rosbridge websocket＋自含網頁，`enable_hmi:=true` 啟用）
- [x] 顯示辨識結果、LLM 狀態、系統狀態（VIP 金色橫幅／黑名單紅色警報／對話流／電池電壓／相機串流）
- [x] 規劃 ROS2 topic / service 與 UI 的介面（訂閱 /face_recognition/result、/user_text、/llm_response、/PowerVoltage；相機經 web_video_server MJPEG）
- [x] 相機畫面串流打通（2026-07-21）：原本 `hmi.launch.py` **沒有啟動 `web_video_server`**，平板永遠停在「相機畫面載入中…」——已在 launch 內加入該節點（新增 `video_port` 參數，預設 8080）並補 `package.xml` 相依。網頁影像話題改為可用 `?video_topic=` 覆寫，預設 `/camera/color/image_raw`（實車 astra）。實測單張快照 102449 bytes JPEG 640×480、3 秒串流 12.3MB
- [ ] 平板實機連線測試（Pi 端已驗證網頁 HTTP 200＋rosbridge websocket＋影像串流實際有影格；平板瀏覽器實連由使用者現場確認中）

### B10. LLM 模型效能比較（✅ 已完成 2026-07-02）

已於 2026-07-02 在筆電上實測完成。環境：Windows 筆電 RTX 3050 Laptop 4GB VRAM、Ollama 0.24.0、`scripts/benchmark_llm_models.py`、每模型 1 次暖機 + 5 次正式、串流 `/api/chat`、銀行場景 system prompt、temperature 0.1、num_ctx 512。

| 模型（Ollama tag） | 基底 | 首次輸出平均 (ms) | 總生成平均 (ms) | 最快 (ms) | 最慢 (ms) | 成功率 |
|---|---|---|---|---|---|---|
| gemma3-bank | gemma3:4b | 2303 | 4700 | 4583 | 4755 | 100% |
| gemma4e2b-bank | gemma4:e2b | 42235 | 47909 | 44838 | 50206 | 100% |
| gemma4e4b-bank | gemma4:e4b | 95066 | 107123 | 100231 | 123839 | 100% |
| qwen2.5:3b | qwen2.5:3b | 2218 | 3076 | 3017 | 3196 | 100% |

關鍵解讀：gemma4 系列模型檔（7.2GB / 9.6GB）超出 4GB VRAM，Ollama 卸載到 CPU 導致慢 20~40 倍；gemma3:4b（4GB）與 qwen2.5:3b（1.9GB）能塞進 VRAM。在此硬體上，**qwen2.5:3b 最快，gemma3:4b 次之，gemma4 系列不適合即時互動**。

- [x] 確認 4 個模型都已 `ollama pull`
- [x] 執行 `python scripts/benchmark_llm_models.py`（Windows 上務必用 `python`，`python3` 是空殼；預設測這 4 個模型，每個跑 5 次）
- [x] 結果 Markdown 表格已產出（上表），可直接貼進報告
- [x] 原始數據已保存於 `benchmark_llm_results.json` 備查
- [ ] （可選）用 `--prompt` 換成其他測試語句，或用 `--runs 10` 提高樣本數，觀察數據是否穩定

## C. 每次更新進度時要填的四欄

- 已完成：已實作且有測試證據
- 待驗證：程式存在但沒有實機結果
- 進行中：現在正在做
- 阻塞：被硬體、依賴、環境卡住

## D. 今天先做什麼

如果你今天只做一件事，先做 A1。

也就是：

1. 在 RPi4 上 `colcon build` 並確認 `/image_raw` 有影像
2. 啟動 `face_recognition` + `face_registration` 節點，用 `/face_registration/register` 服務註冊 VIP / 黑名單
3. 用 `ros2 topic echo /face_recognition/result` 與 `/face_recognition/debug_image` 做現場實測

這一步做完，人臉辨識才能正式從「代碼完成」進到「已驗證」。
