# 架構參考（各套件細節、指令大全、實測數據）

本檔是 CLAUDE.md 的引用檔（2026-07-06 外移，語意不變）。CLAUDE.md 放地圖，這裡放細節；兩邊不重複——改套件行為時更新這裡，改專案狀態時更新 CLAUDE.md。

## 完整專案結構

```
專題/
├── src/                          # ROS 2 workspace（基於 swient/smartnav-bot）
│   ├── smartnav_msgs/            # 訊息/服務/動作定義（ament_cmake）
│   │   ├── msg/  RecognitionResult, AudioData, MapInfo, WaypointInfo
│   │   ├── srv/  RegisterFace, ListMaps, SwitchMap, CreateWaypoint, ListWaypoints, GetWaypoint
│   │   └── action/  CreateMap, GlobalLocalization, Navigate
│   ├── smartnav_vision/          # 人臉辨識（InsightFace buffalo_sc, 512D embedding）
│   ├── smartnav_audio/           # sherpa-onnx 語音（喚醒/ASR/TTS/播放）
│   ├── smartnav_llm/             # LangChain + Ollama 對話式 Agent
│   ├── smartnav_navigation/      # 地圖/地點/導航（原 smartnav_brain 內容，2026-07-01 重構拆出）
│   ├── smartnav_brain/           # 空殼套件（保留給未來 orchestrator/多模態決策）
│   ├── smartnav_hmi/             # 空殼套件（保留給未來人機介面）
│   └── smartnav_bringup/         # 統一啟動（ament_cmake）
│   └── wheeltec/                 # WHEELTEC 廠商套件精選子集（17 套件，2026-07-06 整合，詳見下方章節）
├── scripts/
│   ├── benchmark_llm_models.py   # LLM 延遲基準測試（TTFT/總時長，串流 Ollama /api/chat）
│   ├── download_audio_models.sh  # 下載 sherpa-onnx KWS/ASR/VAD/TTS 模型
│   └── check_handbook.py         # 制度檔健康檢查（行數/禁語/路由完整性）
├── docs/                         # LLM規劃書、專題計畫、部署與重現指南、驗證與實作清單、本檔
├── Modelfile_gemma3 / Modelfile_gemma4e2b / Modelfile_gemma4e4b / Modelfile_qwen
├── benchmark_llm_results.json    # 2026-07-02 實測原始數據
└── README.md、CLAUDE.md、LICENSE(MIT)、photo.jpg、專題報告.pdf/.pptx
```

## 各套件重點

### smartnav_vision（executables: `face_recognition` / `face_registration` / `recognition_text_bridge`）

- **face_recognition_node**：訂閱 `image_topic`（預設 `/image_raw`, BEST_EFFORT QoS）；發布 `/face_recognition/result`（RecognitionResult）與可選 `/face_recognition/debug_image`；服務 `/face_recognition/refresh_cache`（std_srvs/Empty）
  - 參數：`image_topic`、`model_name`（預設 `buffalo_sc`）、`confidence_threshold`（0.5）、`enable_gpu`（true）、`recognition_threshold`（0.6）、`publish_debug_image`（false）、`result_republish_interval`（15.0 秒，同一身分去抖動）
- **face_registration_node**：服務 `/face_registration/register`（smartnav_msgs/srv/RegisterFace：`person_name`/`gender`(M/F/Other)/`person_type`(VIP/BLACKLIST)/`num_samples`）；註冊成功後自動呼叫 refresh_cache
- **recognition_text_bridge_node**：訂閱 `/face_recognition/result` → 轉自然語言 → 發布 `/user_text`；參數 `result_topic`、`user_text_topic`
- **face_engine.py**：InsightFace 封裝與標框（VIP=綠框、黑名單=紅框、訪客=藍框）；**database_manager.py** 管理人員資料與 embedding
- **RecognitionResult.msg** 欄位：`person_uuid`/`person_name`/`gender`/`person_type`(VIP/BLACKLIST/VISITOR)/`confidence`/`timestamp`/`bbox`

### smartnav_audio（executables: `voice_trigger` / `speech_recognizer` / `speech_synthesizer` / `voice_playback`）

- **voice_trigger_node**：KWS 喚醒；發布 `/audio_in`（AudioData）與 `/voice_triggered`（Bool）
- **speech_recognizer_node**：ASR；訂閱 `/audio_in` → 發布 `/user_text`（完整句）與 `/partial_text`（過程）
- **speech_synthesizer_node**：TTS；訂閱 `/speech_text` → 發布 `/audio_out`（AudioData）
- **voice_playback_node**：播放 `/audio_out`
- 模型需先執行 `scripts/download_audio_models.sh` 下載到 `smartnav-bot/src/smartnav_audio/models/`

### smartnav_llm（executable: `llm_service`；launch: `llm.launch.py`）

- **llm_service_node**：訂閱 `/user_text`（std_msgs/String）；發布 `llm_response`、`llm_stream`、`speech_text`
- 參數：`ollama_base_url`（預設 `http://localhost:11434`）、`model_name`（預設 `gemma4:e2b`）、`temperature`（0.0）
- 工具集（**全為導航導向**）：`create_map` / `list_maps` / `switch_map` / `create_waypoint` / `list_waypoints` / `navigate` / `global_localization`
- System prompt：`smartnav-bot/src/smartnav_llm/config/system_prompt.txt`（導航機器人設定）

### smartnav_navigation（executables: `map_service` / `waypoint_service` / `navigation_action`）

- `launch/brain.launch.py`：啟動三節點 + AMCL + slam_toolbox + frontier_explorer + map_server 等
- `launch/nav2.launch.py`：完整 Nav2 棧 + RViz（需 TurtleBot3 或模擬環境）
- `config/`：burger.yaml、empty_map

### smartnav_bringup（launch: `smartnav.launch.py`）

一次啟動 vision + audio + llm + navigation。Launch 參數：
`image_topic`（`/image_raw`）、`enable_gpu`（true）、`ollama_base_url`（`http://localhost:11434`）、`model_name`（`gemma4:e2b`）、`use_sim_time`（false）、`enable_vision` / `enable_audio` / `enable_llm` / `enable_navigation`（皆 true）、`enable_nav2`（**false**，需機器人本體或模擬環境才開）

## WHEELTEC 套件（src/，2026-07-06 整合，全部待實機驗證）

來源：廠商 Humble 世代原始碼（`wheeltec_ros2_src_20260518/`，已 gitignore 的本地素材庫）精選 17 套件，已套用 Jazzy 相容修正（cv_bridge .hpp、死依賴移除、package.xml 補宣告、停用 motion_control）。

| 套件 | 功能 | 關鍵介面 |
|---|---|---|
| turn_on_wheeltec_robot | 底盤驅動（STM32 串口 /dev/wheeltec_controller, 115200） | 發布 `odom`、`imu/data_raw`、超音波；訂閱 `cmd_vel`；主 launch `turn_on_wheeltec_robot.launch.py`；車型由 `config/wheeltec_param.yaml` 的 `car_mode` 決定 |
| wheeltec_robot_msg / serial（serial_ros2） | 底盤自訂訊息／串口函式庫 | turn_on_wheeltec_robot 的依賴 |
| wheeltec_robot_urdf | 全車型 URDF（meshes/ 1.2GB 已 gitignore，scp 補） | robot_state_publisher 用 |
| wheeltec_nav2（wheeltec_robot_nav2） | 各車型調好的 Nav2 參數＋導航 launch | `wheeltec_nav2.launch.py`（**自帶底盤**；map 預設 share/map/WHEELTEC.yaml） |
| wheeltec_slam_toolbox / wheeltec_cartographer | SLAM 薄封裝（引擎吃 apt） | `online_async_launch.py` 建圖（**自帶底盤**） |
| nav2_waypoint_cycle | 巡邏點循環（銀行迎賓可用） | 依賴 apt 內建訊息 |
| ldlidar_ros2 / lslidar_driver / lslidar_msgs | 雷射雷達驅動（底盤預設 lidar_type=ls_N10Plus） | 發布 `/scan` |
| astra_camera / astra_camera_msgs | Orbbec 深度相機（閉源 arm64 SDK，24.04 glibc 未實測） | 發布 `/camera/color/image_raw`（接 smartnav_vision 需 remap 到 `/image_raw`） |
| wheeltec_mic_msg / wheeltec_mic_ros2 | 麥克風陣列＋本地離線命令詞（免金鑰；固定語法非自由語句） | 發布 `voice_words`（bringup 用 topic_tools relay 轉 `/user_text`）、`awake_flag`、`awake_angle` |
| ollama_ros_chat / ollama_ros_msgs | 輕量 Ollama 對話（OpenAI 相容端點，單輪、無工具） | 訂閱 `chat_message`／發布 `chat_response`（JSON）；由 smartnav_llm 的 `ollama_chat_bridge` 接 `/user_text` 與 `/speech_text` |

**TF 注意**：底盤的 odom→base_footprint TF 由 robot_localization EKF 發布，frame 名 **`odom_combined`**；WHEELTEC 導航/SLAM 參數全部配這個名字，與 smartnav_navigation（`odom`）兩鏈不可混用。

**雙版本方案話題流**（bringup 參數選擇）：
```
audio_stack:=wheeltec： 麥克風陣列 → voice_words ──(topic_tools relay)──→ /user_text
llm_stack:=wheeltec：   /user_text ──(ollama_chat_bridge 包 JSON)──→ chat_message
                        chat_response ──(bridge 聚合 is_done)──→ /llm_response ＋ /speech_text
TTS 輸出兩方案共用 smartnav 的 speech_synthesizer → /audio_out → voice_playback
```

**捨棄清單與理由**：navigation2-humble 整包（Humble 版撞 Jazzy，用 apt ros-jazzy-navigation2）；usb_cam／web_video_server／rplidar_ros／ackermann_msgs／tf2_tools（apt 有）；wheeltec_mic_aiui（ASR/NLP/TTS 全綁訊飛雲端金鑰，無金鑰零輸出，且含 demo 金鑰明碼）；tts_make_ros2（一次性工具＋CMake 寫死 x64，RPi4 建置必敗）；largemodel（綁阿里雲付費 key，啟動即退出）；GPS 全組／跟隨／KCF／bodyreader／多機／Qt／gmapping／rtab／rrt／auto_recharge（場景用不到或未宣告依賴）。

## LLM 延遲實測數據（2026-07-02，已驗證）

環境：Windows 筆電 RTX 3050 Laptop 4GB VRAM、Ollama 0.24.0、每模型 1 次暖機 + 5 次正式、串流 `/api/chat`、銀行場景 system prompt、temperature 0.1、num_ctx 512。

| 模型 (Ollama tag) | 基底 | 首次輸出平均(ms) | 總生成平均(ms) | 最快(ms) | 最慢(ms) | 成功率 |
|---|---|---|---|---|---|---|
| gemma3-bank | gemma3:4b | 2303 | 4700 | 4583 | 4755 | 100% |
| gemma4e2b-bank | gemma4:e2b | 42235 | 47909 | 44838 | 50206 | 100% |
| gemma4e4b-bank | gemma4:e4b | 95066 | 107123 | 100231 | 123839 | 100% |
| qwen2.5:3b | qwen2.5:3b | 2218 | 3076 | 3017 | 3196 | 100% |

**關鍵解讀**：gemma4 模型檔 7.2GB/9.6GB 超出 4GB VRAM，Ollama 卸載到 CPU 導致慢 20~40 倍；gemma3:4b（4GB）與 qwen2.5:3b（1.9GB）能塞進 VRAM。此硬體上 qwen2.5:3b 最快、gemma3:4b 次之，gemma4 系列不適合即時互動。原始明細在 `benchmark_llm_results.json`。

## 指令大全（CLAUDE.md 只留最常用，其餘在此）

### 個別節點（RPi4）

```bash
ros2 run smartnav_vision face_recognition --ros-args -p publish_debug_image:=true
ros2 run smartnav_vision face_registration
ros2 run smartnav_vision recognition_text_bridge
ros2 launch smartnav_llm llm.launch.py ollama_base_url:=http://192.168.1.xxx:11434
ros2 launch smartnav_navigation brain.launch.py
```

### LLM 基準測試（Windows 筆電；務必用 python，python3 是空殼）

```powershell
python scripts/benchmark_llm_models.py --models gemma3-bank qwen2.5:3b --runs 5
python scripts/benchmark_llm_models.py --host http://localhost:11434 --output benchmark_llm_results.json
```

### 語音模型下載（RPi4，於專案根目錄）

```bash
bash scripts/download_audio_models.sh   # 下載 KWS/ASR/VAD/TTS 到 smartnav-bot/src/smartnav_audio/models/
```

### 檔案傳輸（Windows ↔ RPi4）

```bash
# 方式一：git（建議）— Windows: commit + push；RPi4: git pull
# 方式二：scp
scp -r "C:/Users/陳佳憲/Desktop/專題/src" xiange@192.168.1.125:~/smartnav-bot/
ssh xiange@192.168.1.125
cd ~/smartnav-bot && colcon build --symlink-install && source install/setup.bash
```

## Key Files Reference

| 檔案 | 用途 |
|------|------|
| `smartnav-bot/src/smartnav_bringup/launch/smartnav.launch.py` | 統一啟動入口（vision+audio+llm+navigation，各模組可獨立開關） |
| `smartnav-bot/src/smartnav_vision/smartnav_vision/face_recognition_node.py` | 人臉辨識節點（InsightFace buffalo_sc, 512D） |
| `smartnav-bot/src/smartnav_vision/smartnav_vision/face_registration_node.py` | 人臉註冊服務節點 |
| `smartnav-bot/src/smartnav_vision/smartnav_vision/recognition_text_bridge_node.py` | 辨識結果 → 自然語言 → `/user_text` |
| `smartnav-bot/src/smartnav_vision/smartnav_vision/face_engine.py` | InsightFace 封裝 + 標框（VIP綠/黑名單紅/訪客藍） |
| `smartnav-bot/src/smartnav_vision/smartnav_vision/database_manager.py` | 人員資料與 embedding 儲存 |
| `smartnav-bot/src/smartnav_llm/smartnav_llm/llm_service_node.py` | LangChain + Ollama Agent（7 個導航工具） |
| `smartnav-bot/src/smartnav_llm/config/system_prompt.txt` | LLM 系統提示詞（目前為導航設定） |
| `smartnav-bot/src/smartnav_msgs/msg/RecognitionResult.msg` | 辨識結果訊息（本專案擴充） |
| `smartnav-bot/src/smartnav_msgs/srv/RegisterFace.srv` | 人臉註冊服務（本專案擴充：gender/person_type） |
| `smartnav-bot/src/smartnav_navigation/launch/brain.launch.py` | 地圖/地點/導航服務 + SLAM/AMCL/frontier_explorer |
| `smartnav-bot/src/smartnav_navigation/launch/nav2.launch.py` | 完整 Nav2 棧 + RViz |
| `smartnav-bot/src/smartnav_llm/smartnav_llm/ollama_chat_bridge.py` | /user_text ↔ ollama_ros_chat 的 JSON 格式橋接（llm_stack:=wheeltec 用） |
| `src/turn_on_wheeltec_robot/config/wheeltec_param.yaml` | 底盤車型（car_mode）與串口/IMU/雷達型號設定 |
| `src/turn_on_wheeltec_robot/wheeltec_udev.sh` | 底盤串口 udev 固定命名腳本 |
| `scripts/benchmark_llm_models.py` | LLM 延遲基準測試（TTFT/總時長，輸出 Markdown + JSON） |
| `scripts/download_audio_models.sh` | sherpa-onnx 語音模型下載 |
| `docs/LLM規劃書.md` | LLM 模型選型與效能分析 |
| `docs/部署與重現指南.md` | 部署與重現步驟 |
| `docs/驗證與實作清單.md` | 驗證狀態與待辦清單 |
| `docs/專題計畫.md` | 完整專題規格 |
| `benchmark_llm_results.json` | 2026-07-02 LLM 實測原始數據 |
