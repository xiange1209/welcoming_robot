# 建置產物 —— colcon build 會重新產生，不進版控
build/
install/
log/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# 打包檔 —— 內容與 welcoming_robot_ws/ 重複，留著會讓 repo 白白肥一倍
car_code_*.tar.gz

# 執行時期記錄
logs/
*.log

# 編輯器
.vscode/
.idea/
*.swp

# 本機密碼設定 —— 絕對不可進版控
local_env.sh
.env.local

# ── 憑證與金鑰 ────────────────────────────────────────
.env
*.pem
*.key
*.crt
*.p12
id_rsa*

# ── 個資：人臉特徵與訪客記錄 ★ 這一段最重要 ──────────
#
# .smartnav/ 是機器人 runtime 目錄 ~/.smartnav 的快照。同一棵樹底下，
# 程式還會寫入 face_database/（512 維人臉向量 .npy ＋ 人臉照片）
# 與 visit_log.db（含姓名與 VIP/BLACKLIST 分類的訪客記錄）。
#
# 人臉特徵在台灣《個人資料保護法》屬**特種個資**，一旦進了公開 repo
# 就無法真正收回（git rm 只改變當前狀態，歷史裡永遠拿得到）。
#
# 現在還沒外洩，只是因為實機還沒跑過人臉註冊。
# 跑過一次之後再 `git add .smartnav`，特種個資就直接上公開 repo 了。
# ★ 同步 .smartnav 時請用具名路徑 add，不要整個目錄加。
.smartnav/face_database/
.smartnav/visit_log.db
*.db
*.sqlite
*.sqlite3
user_registry.json
*.npy
*.pkl

# ── 測試影像：可能拍到人或室內環境 ────────────────────
test_frames/
*.jpg
*.jpeg
*.png
# 若之後 HMI 需要靜態圖片，用 ! 開例外，不要整條拿掉

# ── 打包檔（泛用）──────────────────────────────────
*.tar.gz
*.tgz
*.zip

# ── 內部開發文件不隨 repo 發布（見 README 的「開發文件」節）────
#
# 這些是逐日的除錯記錄與操作手冊，對外部讀者沒有參考價值，
# 而且其中的韌體分析對象是廠商未公開發布的原始碼。
# 它們隨部署包（tar）提供給實際操作的人。
docs/
HANDOFF_*.md
AGENT交接指南.md
README_本機開發.md
本機開發與驗收.md
廠商原始碼分析_*.md
# 同性質：引用了廠商未公開發布的 STM32 韌體原始碼（含行號與片段）。
# 隨部署 tar 提供給實際操作的人，不隨公開 repo 發布。
韌體限制_*.md
明天怎麼跑_*.md
# ★ 2026-08-07：原本只擋「明天怎麼跑_」，擋不到「明天驗證清單_」「下週驗證清單_」——
#   這些檔含 HMI token 用法、內網拓撲、逐日除錯細節，一次 git add -A 就會推上公開 repo。
#   改用寬鬆樣式，任何「*驗證清單*」都擋。
*驗證清單*.md
HMI_驗證步驟.md
專題可行性評估_*.md

# ── 實驗逐筆記錄與 runtime 狀態 ────────────────────
#
# replay_*.csv 是重播時每一筆 feedback 的逐筆記錄（除錯用，
# 一次上千列）。報告要用的是整理過的圖表，不是原始 log。
replay_*.csv
odom_gyro_check_*.csv
*.bak_*
.smartnav/nav_state.json

# 2026-08-14：車上實測產出的工作文件。與上面 HANDOFF_*/驗證清單* 同一個理由——
# 它們是給實際操作機器人的人看的，隨部署 tar 提供，不隨公開 repo 發布。
MPPI診斷_*.md
MPPI實測_*.md
ASR接線_*.md
LLM工具選擇_*.md
交接_*.md

# 8/17：雙麥克風原始錄音（實驗素材，含實驗室環境音，不進公開版控；Pi 上保留）
maprun/tools_0817/rec/
技術總整理.md

# 8/17：模型選型調查與研究數據索引。與上面同一個理由——內部開發文件，
# 隨部署 tar 提供。索引檔還列出了逐日 log 與量測檔的實際路徑。
模型選型對比_*.md
ASR繁體中文選型_*.md
研究數據索引.md
LLM修復_*.md
HMI閒置連線_*.md
HMI車輛圖示_*.md
雙麥克風_*.md
